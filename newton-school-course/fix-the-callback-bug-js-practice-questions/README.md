# Fix the Callback Bug - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `keu3qfboakqt`  
**Submission Time:** 2026-09-17T09:17:20.387Z  

## Problem Statement

Riya works as a junior software engineer, and today she is debugging a piece of code inside a billing system.
The system uses a Higher-Order Function (HOF) named calculateBill, which expects a callback that returns a processed value.

Your job is to fix the callback bug

 

our Task:

Fix the callback so that:

 
 It correctly calculates GST = 18% of the amount

 

 
 It returns the final bill = amount + GST

 

### Input
The input will contain one number (the original amount):
This number should be treated as the amount passed to the callback.

### Output
Print the final bill after adding 18% GST.

### Example
Input:
500

Processing:
GST = 18% of 500 = 90
Final Bill = 500 + 90 = 590

Output:
590

## Solution

```js
function calculateBill(amount, callback) {
    return callback(amount);
}

function applyGST(amount) {
    const gst = amount * 0.18;
    return amount + gst;
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
