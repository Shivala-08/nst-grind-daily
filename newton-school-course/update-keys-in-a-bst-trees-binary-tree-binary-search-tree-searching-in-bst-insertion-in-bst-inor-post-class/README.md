# Update keys in a BST - Trees, Binary Tree, Binary Search Tree, Searching in BST, Insertion in BST, Inor ... - Post Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `kxnq7i0gl0ig`  
**Submission Time:** 2026-09-17T07:20:06.242Z  

## Problem Statement

Given the root of a binary search tree, modify it such that every node is updated to contain the sum of all greater keys present in the BST.

For example, BST shown on the left should be updated to BST on the right

Note: In this modification process, each node in the binary search tree will be updated to contain a value equal to its original value plus the sum of all greater nodes present in the BST.

### Input
Your Task
Since this is a functional problem, you do not need to handle input or output. Your task is to complete the function updateBST(), which takes the root as the input parameter.

Custom Input
The first line of input contains an integer, N, representing the number of nodes in the tree.
The next line consists of space-separated integers denoting the level order traversal of the tree, where non-negative values represent node values, and -1 indicates a null node.
Note: Once a node is marked as -1, no further information about its children is provided.

### Output
Return the root of the updated BST.

Custom Output
Function will print the level order traversal of the updated BST.

### Example
Input:
6
5 3 6 2 4 -1 7

Output:
18 25 13 27 22 -1 7
Explanation

Each node value will be updated as :
2 -&gt; 2 + 3 + 4 + 5 + 6 + 7 = 27
3 -&gt; 3 + 4 + 5 + 6 + 7 = 25
4 -&gt; 4 + 5 + 6 + 7 = 22
5 -&gt; 5 + 6 + 7 = 18
6 -&gt; 6 + 7 = 13
7 -&gt; 7

Input:
10
50 25 75 12 37 62 87 -1 -1 30 -1 60 70

Output:
404 496 162 508 441 294 87 -1 -1 471 -1 354 232

## Solution

```js
'''
class Node:
    def __init__(self, val, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right 
'''    
def updateBST(root):
    # Helper function to perform reverse in-order traversal
    def reverse_inorder(node):
        nonlocal total_sum
        if not node:
            return
        
        # Traverse the right subtree first (greater values)
        reverse_inorder(node.right)
        
        # Update the current node's value with the running sum
        total_sum += node.val
        node.val = total_sum
        
        # Traverse the left subtree (smaller values)
        reverse_inorder(node.left)

    total_sum = 0
    reverse_inorder(root)
    return root
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
