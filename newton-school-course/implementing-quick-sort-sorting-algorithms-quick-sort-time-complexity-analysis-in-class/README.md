# Implementing Quick Sort - Sorting Algorithms, Quick Sort, Time Complexity Analysis - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `7rgkdotnp2a9`  
**Submission Time:** 2026-09-17T09:35:43.076Z  

## Problem Statement

Given an array nums of n positive integers, sort the array using the Quick Sort algorithm.

### Input
User Task:
Since this is a functional problem you don't have to worry about the input. You just have to complete the function quick_sort() which contains following arguments:-

* nums: input array

* lo: starting index of array

* hi: ending index of array

Custom Input:

* The first line contains an integer T — number of test cases.

* For each test case:

* The first line contains an integer n — size of the array.

* The second line contains n space-separated integers representing the array nums.

### Output
You don't need to return anything. Modify the array in-place.

### Example
Sample Input:

2
3
3 1 2
3
4 5 6

Sample Output:

1 2 3
4 5 6

Explanation:
There are 2 test cases.

For the first test case:
The array is [3, 1, 2]. After applying sorting, it becomes [1, 2, 3].

For the second test case:
The array is [4, 5, 6]. It is already sorted, so the output remains [4, 5, 6].

## Solution

```py
def quick_sort(nums, lo, hi):
    if lo >= hi:
        return
        
    # Use the middle element as the pivot to avoid worst-case performance on sorted arrays
    pivot = nums[(lo + hi) // 2]
    i = lo - 1
    j = hi + 1
    
    # Hoare's partition scheme
    while True:
        i += 1
        while nums[i] < pivot:
            i += 1
            
        j -= 1
        while nums[j] > pivot:
            j -= 1
            
        if i >= j:
            break
            
        # Swap elements out of place
        nums[i], nums[j] = nums[j], nums[i]
        
    # Recursively sort the two partitions
    quick_sort(nums, lo, j)
    quick_sort(nums, j + 1, hi)
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
