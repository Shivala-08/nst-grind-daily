# Triplet-Sum - ADA | Contest 1 | Set 1 | ADYPU | 25-29

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `jv9byxkvn4so`  
**Submission Time:** 2026-09-17T11:05:48.472Z  

## Problem Statement

Given an integer array nums and an integer target, determine whether there exist three different indices i, j, and k such that:

nums[i] + nums[j] + nums[k] == target

### Input
The first line contains an integer t.

For each testcase:

* The first line contains two integers n and target — the number of elements in the array and the target sum.

* The second line contains n space-separated integers — the elements of the array nums.

### Output
For each testcase:

* For each test case, print YES if there exists a triplet of integers whose sum equals target, otherwise print NO.

### Example

Input:
2
6 12
2 5 6 4 3 5
5 20
1 8 2 5 4

Output:
YES
NO

Explanation:
In the first testcase, the triplet (2, 5, 5) sums up to 12.
In the second testcase, no triplet sums up to 20.

## Solution

```py
import sys

def solve():
    input_data = sys.stdin.read().split()
    if not input_data:
        return
    
    t = int(input_data[0])
    idx = 1
    
    results = []
    for _ in range(t):
        if idx >= len(input_data):
            break
        n = int(input_data[idx])
        target = int(input_data[idx+1])
        idx += 2
        
        nums = []
        for _ in range(n):
            nums.append(int(input_data[idx]))
            idx += 1
            
        found = False
        # Brute-force through all triplets (i, j, k)
        for i in range(n):
            for j in range(i + 1, n):
                for k in range(j + 1, n):
                    if nums[i] + nums[j] + nums[k] == target:
                        found = True
                        break
                if found:
                    break
            if found:
                break
                
        results.append("YES" if found else "NO")
        
    print('\n'.join(results))

if __name__ == '__main__':
    solve()
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
