// The platform provides 'name', 'domain', and 'level' automatically.
// Just create the formatted string and print it.

let finalOutput = `Dev Club | Name: ${name} | Domain: ${domain} | Level: ${level}`;
console.log(finalOutput);