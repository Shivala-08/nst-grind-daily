# Implementation of MAX Priority Queue - Heaps, Heap, Insertion in Heap, Deletion in Heap - Post Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `52kmkzv7fn4z`  
**Submission Time:** 2026-09-17T10:09:08.015Z  

## Problem Statement

You are tasked with implementing a Priority Queue (based on a Max-Heap) that supports the following operations:

 
 push(x): Insert a new element x into the Priority Queue.

 

 
 getMax(): Return the maximum element from the Priority Queue without removing it.

 

 
 pop(): Remove and return the maximum element from the Priority Queue.

 

 
 empty(): Return True if the Priority Queue is empty, otherwise False.

 

 
 size(): Return the number of elements in the Priority Queue.

 

### Input
User task:
Since this will be a functional problem, you don't have to take input. You just have to complete the functions in class Priority Queue.

* push(x): Insert a new element x into the Priority Queue.

* getMax(): Return the maximum element from the Priority Queue without removing it.

* pop(): Remove and return the maximum element from the Priority Queue.

* empty(): Return True if Priority Queue is empty otherwise False.

* size(): Return the size of the Priority Queue.

Custom Input:
First line contains number of operations N
Next N lines contains queries of following types:

* Type 0: Push a value onto the data structure.

* Type 1: Return the maximum value currently in the data structure.

* Type 2: Pop and return the maximum value from the data structure.

* Type 3: Return True is Priority Queue is empty otherwise False.

* Type 4: Return the size of the Priority Queue.

### Output
For the given queries, your implementation should return the following:
Type 1: Return the maximum element from the PriorityQueue (without removing it).
Type 2: Pop and return the maximum element from the PriorityQueue.
Type 3: Return True if the PriorityQueue is empty, otherwise return False.
Type 4: Return the current size of the PriorityQueue.

### Example
Input:
9
0 5
0 3
0 7
3
2
1
2
1
4
Output:
False
7
5
5
3
1
Explanation:

* 0 5 → Insert 5 → Heap: [5]

* 0 3 → Insert 3 → Heap: [5, 3]

* 0 7 → Insert 7 → Heap: [7, 3, 5] (Heap property maintained)

* 3 → Is heap empty? → False

* 2 → Pop max (7 removed) → Return 7 → Heap: [5, 3]

* 1 → Peek max → 5

* 2 → Pop max (5 removed) → Return 5 → Heap: [3]

* 1 → Peek max → 3

* 4 → Current size of heap → 1

## Solution

```py
import heapq

class PriorityQueue:
    def __init__(self):
        self.heap = []

    def push(self, x):
        # Push the negative value to simulate a Max-Heap using Python's Min-Heap
        heapq.heappush(self.heap, -x)

    def getMax(self):
        if self.heap:
            # The maximum element is at the root (index 0), negated back to positive
            return -self.heap[0]
        return None

    def pop(self):
        if self.heap:
            # Pop the root and negate it back to its original positive value
            return -heapq.heappop(self.heap)
        return None

    def empty(self):
        return len(self.heap) == 0

    def size(self):
        return len(self.heap)
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
