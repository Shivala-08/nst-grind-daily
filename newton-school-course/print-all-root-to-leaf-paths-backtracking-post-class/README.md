# Print all root to leaf paths - Backtracking - Post Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `qx4y2466uleb`  
**Submission Time:** 2026-09-17T07:45:18.741Z  

## Problem Statement

Given the root of a binary tree, return all root to leaf paths.

 

### Input
Since this is a functional problem, you don't have to input anything. You just need to complete the rootToLeafPaths().

You are given root node of the binary tree as function parameter.

### Output
Return all the paths from root to leaf .

Custom Input Output
First line contains an integer value 'N' indicating number of nodes(including null) in binary tree.
Second line will be a string representing the tree as described below: 
The values in the string are in the order of pre-order traversal of the tree where, numbers denote node values, and -1 denotes NULL child.

Note: If a node has been declared Null using -1, no information about its children will be given further in the array.

### Example
Input:
11
50 25 -1 -1 12 37 -1 -1 30 -1 -1

Output:
50 25
50 12 37
50 12 30

## Solution

```py
'''
class Node:
    def __init__(self, data, left=None, right=None):
        self.data = data
        self.left = left
        self.right = right
'''
def rootToLeafPaths(root):
    paths = []

    def dfs(node, path):
        if not node:
            return
        
        # Append current node's data to the path
        path.append(str(node.data))
        
        # If it's a leaf node, collect the path
        if not node.left and not node.right:
            paths.append(" ".join(path))
        else:
            # Continue traversal for left and right children
            dfs(node.left, path)
            dfs(node.right, path)
            
        # Backtrack
        path.pop()

    dfs(root, [])
    return paths
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
