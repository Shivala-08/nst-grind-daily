# JS Step Counts - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `re2qw4pcvaz9`  
**Submission Time:** 2026-09-17T10:27:09.347Z  

## Problem Statement

You are developing a data analysis tool for a fitness application that tracks users&#39; daily step counts. The application needs to process an array of step counts to filter and transform the data based on specific criteria. The goal is to analyze the step counts to provide insights into users&#39; activity levels.

Function Definitions

processNumbers Function: This function processes an array of numbers (step counts) based on certain conditions. It takes two parameters:

- An array of numbers (numbers), representing daily step counts.

- A callback function (callback) that defines how to transform each valid step count.

The function iterates through the array and applies the following logic:

It skips any odd step counts that are greater than 10 (indicating that these counts may be outliers or errors).

If it encounters an odd step count that is less than or equal to 10, it stops processing further numbers (indicating that the user may have had an inactive day).

For valid step counts, it applies the callback function to transform the number (e.g., doubling the count or adding a fixed value) and stores the result in a new array.

Callback Functions:

double Function: This function takes a number and returns that number multiplied by 2. It is used to analyze the step counts by doubling them to represent a hypothetical scenario (e.g., if the user had twice the steps).

 

### Input
A number of integers seperated by spaces

### Output
An array representing processed step counts

### Example
Example Usage
Input:
2 4 6 8 11 12 15 3

Output: 
[ 4, 8, 12, 16, 24 ]

## Solution

```js
function processNumbers(numbers, callback) {
  const result = [];

  for (let i = 0; i < numbers.length; i++) {
    const count = numbers[i];

    // Check if the number is odd
    if (count % 2 !== 0) {
      if (count > 10) {
        // Skip odd step counts greater than 10
        continue;
      } else {
        // Stop processing if odd step count is less than or equal to 10
        break;
      }
    }

    // For valid step counts (even numbers), apply the callback and store
    result.push(callback(count));
  }

  return result;
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
