# Employee Salary Management System - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `94m6u2iej76i`  
**Submission Time:** 2026-09-17T09:33:06.546Z  

## Problem Statement

You are tasked with building a salary management system for a company. The company maintains an array of employee objects, each containing:

id: A unique identifier for each employee (integer).

name: The name of the employee (string).

salary: The salary of the employee (integer).

You need to implement two features in the system:

1. Feature 1 - Find Employee by ID:
If the employee is found

print &lt;code&gt;"Employee found"&lt;/code&gt;.

If the employee is not found, print &lt;code&gt;"Employee not found"&lt;/code&gt;.

### Input
An array of employee objects, where each object has id, name, and salary properties.
A specific employee ID to search for in the list.
A salary to check if it exists among the employees.

### Output
For Feature 1, Print the employee's details if the employee is found by ID, or "Employee not found" if the employee does not exist.

### Example
Example:
Input:
[ { "id": 101, "name": "John Doe", "salary": 50000 }, { "id": 102, "name": "Jane Smith", "salary": 60000 }, { "id": 103, "name": "Sam Green", "salary": 55000 } ]
102
55000
Output:
"Employee found"

## Solution

```js
function manageEmployeeSalary(employees, employeeId, salaryToCheck) {
  // Feature 1: Find Employee by ID
  const employee = employees.find(emp => emp.id === employeeId);
  
  if (employee) {
    console.log("Employee found");
  } else {
    console.log("Employee not found");
  }

  // Note: The problem description cuts off before explaining Feature 2 (which likely uses salaryToCheck). 
  // If the tests fail and expect a second line of output for the salary, you can add something like this:
  // const salaryExists = employees.some(emp => emp.salary === salaryToCheck);
  // console.log(salaryExists ? "Salary found" : "Salary not found");
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
