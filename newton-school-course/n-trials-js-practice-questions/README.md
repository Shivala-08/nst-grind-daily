# N trials - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `g2qte4c9vsl0`  
**Submission Time:** 2026-09-17T09:29:53.873Z  

## Problem Statement

Complete the below higher‑order function that takes:

 * A zero‑argument function fn

 * A natural number n

function runNTimes(fn, n)

Your runNTimes the function should invoke fn() exactly n times. Collect each return value, apply the JSON.stringify() to the return value, and print each result in a new line in the order of invocation.

Constraints:

 * 1 &le; n &le; 10000

 * Each invocation of fn() will return a value that can be serialised in JSON (number, string, boolean, etc.)

### Input
Line 1: A string representing a JavaScript zero‑argument arrow function, e.g. ()=&gt;42 or ()=&gt;{return "hi"}

Line 2: An integer n

### Output
Print the JSON-stringified result of each of the n calls to fn(), one per line.

### Example
Example:
Input:
()=&gt;42 
3 

Output:
42
42
42

## Solution

```js
function runNTimes(fn, n) {
    for (let i = 0; i < n; i++) {
        console.log(JSON.stringify(fn()));
    }
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
