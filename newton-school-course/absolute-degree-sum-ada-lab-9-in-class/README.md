# Absolute degree sum - ADA Lab 9 - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `xn3qcfs726ze`  
**Submission Time:** 2026-09-17T10:13:12.661Z  

## Problem Statement

Given a directed graph with n vertices and m edges, print the sum of the absolute differences between the in-degree and out-degree of all vertices.

### Input
User Task
This is a function problem. You don’t have to take any input. You are required to complete the function sumOfAbsoluteDegreeDiff() that takes an integer n (number of vertices) and a 2D list edgeList (edge list of the directed graph) as parameters, and returns an integer representing the sum of absolute differences between in-degree and out-degree for all vertices.

Custom Input
The first line contains two integers, n and m, representing the number of vertices and the number of directed edges respectively.
The next m lines each contain two integers u and v, representing a directed edge from vertex u to vertex v.

### Output
Return a single integer — the total sum of |indegree(v) − outdegree(v)| for all vertices v from 0 to n-1.

### Example
Input
4 4
0 1 
0 2 
1 2 
2 3 

Output
4

Explanation:

Let's compute in-degree and out-degree for each vertex:

* Vertex 0 → out: 2 (to 1, 2), in: 0 → |0 − 2| = 2

* Vertex 1 → out: 1 (to 2), in: 1 (from 0) → |1 − 1| = 0

* Vertex 2 → out: 1 (to 3), in: 2 (from 0, 1) → |2 − 1| = 1

* Vertex 3 → out: 0, in: 1 (from 2) → |1 − 0| = 1

Total sum = 2 + 0 + 1 + 1 = 4

## Solution

```py
def sumOfAbsoluteDegreeDiff(n, edgeList):
    # Initialize arrays to store in-degrees and out-degrees for each vertex
    in_degree = [0] * n
    out_degree = [0] * n
    
    # Calculate degrees based on the edges
    for u, v in edgeList:
        out_degree[u] += 1
        in_degree[v] += 1
        
    # Calculate the sum of absolute differences
    total_sum = 0
    for i in range(n):
        total_sum += abs(in_degree[i] - out_degree[i])
        
    return total_sum
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
