# Convert Sorted Array to Balanced BST - Deletion In BST - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `0o4vte2a57vk`  
**Submission Time:** 2026-09-17T09:36:25.407Z  

## Problem Statement

You are given an integer array nums of size n, sorted in strictly increasing order.

Construct a height-balanced Binary Search Tree containing all the elements of nums.

A Binary Search Tree is a binary tree in which, for every node, all values in its left subtree are smaller than the node&#39;s value and all values in its right subtree are greater than the node&#39;s value.

A binary tree is height-balanced if, for every node, the heights of its left and right subtrees differ by at most 1.

There may be multiple valid answers. Any height-balanced Binary Search Tree containing all the given values will be accepted.

User Task:

Complete the function sortedArrayToBST(nums). The function should return the root of a valid height-balanced Binary Search Tree constructed from the given sorted array.

### Input
The first line contains a single integer n — the number of elements in the array.

The second line contains n integers nums_1, nums_2, ..., nums_n — the elements of the array in strictly increasing order.

### Output
Return the root of any valid height-balanced Binary Search Tree containing every element of nums exactly once.

Since more than one valid tree may exist, the returned tree will be checked for correctness using a custom checker.

Custom Output:
Although your function only needs to return the root of the constructed tree, for evaluation purposes the returned tree will be represented using two traversals.

The first line will contain the preorder traversal of the returned tree.

The second line will contain the inorder traversal of the returned tree.

### Example
Example 1:
Input5
-10 -3 0 5 9Output0 -10 -3 5 9
-10 -3 0 5 9Explanation
The constructed height-balanced BST has 0 as the root. Its left subtree is rooted at -10 with -3 as its right child, and its right subtree is rooted at 5 with 9 as its right child.

The first line contains the preorder traversal of the returned tree.
The second line contains the inorder traversal of the returned tree.

Example 2:
Input2
1 3Output1 3
1 3Explanation
The constructed height-balanced BST has 1 as the root and 3 as its right child.

The first line contains the preorder traversal of the returned tree.
The second line contains the inorder traversal of the returned tree.

## Solution

```js
'''
class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left = None
        self.right = None
'''
def sortedArrayToBST(nums):
    def helper(left, right):
        if left > right:
            return None
        
        # Find the middle element to maintain a balanced tree
        mid = (left + right) // 2
        root = TreeNode(nums[mid])
        
        # Recursively construct the left and right subtrees
        root.left = helper(left, mid - 1)
        root.right = helper(mid + 1, right)
        
        return root
        
    # Call the helper function with the full bounds of the array
    return helper(0, len(nums) - 1)
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
