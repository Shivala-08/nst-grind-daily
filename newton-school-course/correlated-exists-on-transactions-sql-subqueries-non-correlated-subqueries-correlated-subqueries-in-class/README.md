# Correlated EXISTS on Transactions - SQL Subqueries, Non-Correlated Subqueries, Correlated Subqueries - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `8r59vr48wbsm`  
**Submission Time:** 2026-09-24T07:41:39.890Z  

## Problem Statement

Scenario: The compliance team needs to audit account activity. They want a list of accounts that have at least one transaction on record, accounts with no transactions at all, and &mdash; for risk review &mdash; accounts where at least one transaction exceeds that account&#39;s own average transaction amount.

Table: accounts

+----------------+---------------+
| Column | Type |
+----------------+---------------+
| account_id | INT (PK) |
| account_holder | VARCHAR(50) |
| branch | VARCHAR(20) |
| account_type | VARCHAR(20) |
| account_status | VARCHAR(10) |
| balance | DECIMAL(12,2) |
| opened_date | DATE |
+----------------+---------------+

Table: transactions

+------------+----------------------+
| Column | Type |
+------------+----------------------+
| txn_id | INT (PK) |
| account_id | INT (FK &rarr; accounts) |
| txn_amount | DECIMAL(12,2) |
| txn_type | VARCHAR(10) |
| txn_date | DATE |
+------------+----------------------+

Tasks:

1. Retrieve the account_holder, branch, and balance of all Active accounts that have at least one transaction whose amount is greater than the average transaction amount for that same account. Use a correlated EXISTS subquery and a correlated aggregate subquery to calculate the account-specific average transaction amount.

### Output
 account_holder | branch | balance 
----------------+-----------+-----------
 Amit Sharma | Mumbai | 45200.00
 Ravi Kumar | Mumbai | 310000.00
 Karan Patel | Bangalore | 62000.00
(3 rows)

## Solution

```js
SELECT account_holder, branch, balance
FROM accounts a
WHERE account_status = 'Active'
  AND EXISTS (
      SELECT 1 
      FROM transactions t
      WHERE t.account_id = a.account_id
        AND t.txn_amount > (
            SELECT AVG(txn_amount) 
            FROM transactions 
            WHERE account_id = a.account_id
        )
  );
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
