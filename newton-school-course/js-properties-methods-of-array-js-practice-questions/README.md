# JS Properties & Methods of Array - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `1rccocgvox9y`  
**Submission Time:** 2026-09-17T10:15:14.479Z  

## Problem Statement

This task involves writing a JavaScript function named arrayProperties within a given boilerplate. This function accepts two parameters, arr1 and arr2, each being an array of fruit names. The function will perform a series of array manipulations and output specific results to the console at each step. It&#39;s important to follow the order of the steps provided, as each step builds upon the previous one. Here&#39;s a breakdown of the steps to be implemented in the function:

Steps:

 * Concatenate Arrays: Create a new array arr3 by concatenating arr2 followed by arr1.

 * Push and Print: Push the entire arr2 array as a single element into arr3. Print arr3 to the console.

 * Pop and Reverse: Remove the last item from arr3 (which is the pushed in the before step: arr2 array). Print the reverse of arr3 to the console.

 * Find and Print Index: Find the last index of the item "orange" in arr3. Print this index to the console. 

 * Slice and Print Length: Create a slice of arr3 that includes the first item in the array up to the third item. Print the length of this sliced array to the console.

 * Join and Print: Join the elements of the sliced array into a string, separated by commas. Print this string to the console.

 

### Input
arr1: An array of fruit names (string array).
arr2: Another array of fruit names (string array).

The input takes two arrays and a space in between them. 

### Output
The function outputs several results to the console at different steps:
Concatenate Arrays: No output to the console.
Push and Print: Prints the array arr3 after pushing arr2 as a single element.
Pop and Reverse: Prints the reverse of arr3 after popping the last element.
Find and Print Index: Prints the index of the last occurrence of "orange" in arr3.
Slice and Print Length: Prints the length of the sliced array (first to third item of arr3).
Join and Print: Prints a string made by joining elements of the sliced array, separated by commas.

### Example
Sample Input:
["pineapple", "orange", "banana"] ["apple", "orange", "mango"]

Sample Output:
["apple", "orange", "mango", "pineapple", "orange", "banana", ["apple", "orange", "mango"]]
["banana", "orange", "pineapple", "mango", "orange", "apple"]
4
3
banana,orange,pineapple

## Solution

```js
function arrayProperties(arr1, arr2) {
    // 1. Concatenate Arrays: arr2 followed by arr1
    let arr3 = arr2.concat(arr1);
    
    // 2. Push and Print: push the entire arr2 array as a single element
    arr3.push(arr2);
    console.log(arr3);
    
    // 3. Pop and Reverse: remove the last item and reverse the array
    arr3.pop();
    arr3.reverse();
    console.log(arr3);
    
    // 4. Find and Print Index: last index of "orange"
    console.log(arr3.lastIndexOf("orange"));
    
    // 5. Slice and Print Length: first item up to the third item
    let slicedArr = arr3.slice(0, 3);
    console.log(slicedArr.length);
    
    // 6. Join and Print: join elements separated by commas
    console.log(slicedArr.join(","));
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
