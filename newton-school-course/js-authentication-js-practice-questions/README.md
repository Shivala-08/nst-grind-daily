# Js authentication - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `4r7o25j9vfu7`  
**Submission Time:** 2026-09-17T13:16:57.766Z  

## Problem Statement

Write a function authenticateUser(username, password, users) that validates the given username and password against a users object containing multiple user credentials. The function must return a promise that resolves or rejects exactly after 3 seconds as follows:

 * If the username exists in the users object and the password matches:

 After 3 seconds, resolve the promise with the message: "Login successful for user: username" (in place of username provide the username variable).

 * If the username does not exist or the password is incorrect:

 After 3 seconds, reject the promise with the message: "Authentication failed. Invalid username or password.".

 * The array of users are already given.

Below is an example user object

const users = {

 "admin": "password",

 "user1": "password1",

 "user2": "password2",

 "user3": "password3",

 "user4": "password4",

} 

### Input
This is a functional problem. You do not need to take any input from the user.

### Output
The promise must resolve or reject exactly after 3 seconds to pass the test cases.

### Example
Sample Input 1: note(for testing you dont need to take any input the input is already taken only the output should match)
admin password123
user1 wrongpassword
unknownUser password123

Sample Output:

Promise resolved
Login successful for user: admin
Promise resolved
Authentication failed. Invalid username or password.
Promise resolved
Authentication failed. Invalid username or password.

## Solution

```js
function authenticateUser(username, password, users) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      // Check if user exists in the object and the password matches
      if (users[username] && users[username] === password) {
        resolve(`Login successful for user: ${username}`);
      } else {
        reject("Authentication failed. Invalid username or password.");
      }
    }, 3000);
  });
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
