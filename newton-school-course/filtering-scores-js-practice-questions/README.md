# Filtering Scores - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `i7zpuo4n4e6b`  
**Submission Time:** 2026-09-17T10:33:54.389Z  

## Problem Statement

You are given an array of numerical scores and need to implement the function processScores. This function filters the scores based on the provided callback functions.

The function should take in:

 An array of numbers representing the scores.

 n callback functions, where each function returns a truthy or falsy value based on certain logic.

Each callback function should be applied to every element in the scores array, if all the callback functions return a truthy value for a certain element of the scores array, then ONLY include it in the result array.

Note:- do not hamper the order of elements in the result.

### Input
First line of input is an array of numerical scores.
A series of callback functions, each written on a new line in a compact single-line format.
last line is a string "done" representing end of input

### Output
a modified array after subjecting each element to given callback functions

### Example
Input:
[30, 45, 60, 75, 80, 90]
function evenNumbers(score) { return score % 2 === 0;}
function greaterThan50(score) { return score &gt; 50;}
done

Output:
[ 60, 80, 90 ]

Explanation:
The function applies two filtering conditions sequentially:

It first keeps only even numbers, resulting in [30, 60, 80, 90].
Then, it keeps numbers greater than 50, giving the final output [60, 80, 90].

## Solution

```js
function processScores(scores, ...callbacks) {
    return scores.filter(score => {
        // Check if every callback function returns a truthy value for the score
        return callbacks.every(callback => callback(score));
    });
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
