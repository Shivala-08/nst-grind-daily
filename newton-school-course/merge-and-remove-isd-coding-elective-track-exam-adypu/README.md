# Merge and Remove - ISD - Coding -  Elective Track Exam | ADYPU

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `3vt894pu7jus`  
**Submission Time:** 2026-09-17T11:13:24.741Z  

## Problem Statement

Write a function mergeAndRemove that takes 2 objects and multiple keys as parameters. 

Requirements

 Merge two objects obj1 and obj2 into a single object. 

 If both objects have the same key, then the value of key in obj2 will override

 Remove all keys provided in keysToRemove(containing multiple keys) from the merged object. 

 Return the final object after performing both operations. 

parameter: 1 (Obj1) Object

parameter: 2 (Obj2) Object

parameter: 3 Multiple keys

Note: Do not call the function

### Input
The first input line contains a JSON object (obj1).
The second input line contains another JSON object (obj2).
The third input line contains a space- separated multiple keys to be removed from the merged object.

### Output
The function outputs the merged object with the specified keys removed.

### Example
Input:
{"a": 1, "b": 2}
{"b": 3, "c": 4, "d": 5}
b c

Output:
{a: 1, d: 5}

## Solution

```js
function mergeAndRemove(obj1, obj2, ...keysToRemove) {
    // Merge objects with obj2 overriding obj1
    const mergedObj = { ...obj1, ...obj2 };
    
    // Remove the specified keys
    for (let key of keysToRemove) {
        delete mergedObj[key];
    }
    
    // Return the final object
    return mergedObj;
}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
