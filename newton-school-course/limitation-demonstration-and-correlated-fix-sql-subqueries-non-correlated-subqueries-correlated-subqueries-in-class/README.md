# Limitation Demonstration and Correlated Fix - SQL Subqueries, Non-Correlated Subqueries, Correlated Subqueries - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `vepgd8efiyrm`  
**Submission Time:** 2026-09-24T07:41:18.336Z  

## Problem Statement

Scenario: The branch manager wants to know which accounts perform above average within their own branch &mdash; not compared to the bank as a whole. A non-correlated subquery has already been written but only measures against the global average. Your task is to demonstrate its limitation and fix it.

Table: accounts

+----------------+---------------+
| Column | Type |
+----------------+---------------+
| account_id | INT (PK) |
| account_holder | VARCHAR(50) |
| branch | VARCHAR(20) |
| account_type | VARCHAR(20) |
| account_status | VARCHAR(10) |
| balance | DECIMAL(12,2) |
| opened_date | DATE |
+----------------+---------------+

Table: transactions

+------------+----------------------+
| Column | Type |
+------------+----------------------+
| txn_id | INT (PK) |
| account_id | INT (FK &rarr; accounts) |
| txn_amount | DECIMAL(12,2) |
| txn_type | VARCHAR(10) |
| txn_date | DATE |
+------------+----------------------+

Tasks:

1. Write a query that returns the account holder, branch, and balance for all accounts whose balance is greater than the average balance of their own branch. Use a correlated subquery to calculate the branch-specific average for each account.

### Output
 account_holder | branch | balance 
----------------+-----------+-----------
 Priya Mehta | Bangalore | 128000.00
 Ravi Kumar | Mumbai | 310000.00
(2 rows)

## Solution

```js
SELECT account_holder, branch, balance
FROM accounts a
WHERE balance > (
    SELECT AVG(balance)
    FROM accounts
    WHERE branch = a.branch
);
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
